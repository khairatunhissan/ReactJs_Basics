Exercise-1
What are the difference between forEach and map

Exercise-2
Among these which are polyfilled and why
1.Arrow functions
2.Async functions
3.Promise
4.Destructuring
5.Array#findIndex
6..Classes
7.Array.from

Exercise-3
Among these which are compiled and why
1.constants
2.default parameters
3.map
4.function bind
5.indexOf
6.fill
7.some
8.every
9.reduce
10.spread
11.filter

Exercise-4
Main difference between compiling and polyfilling

Exercise-5
Which part are compiled and polyfilled
const getProfile = username => {
  return fetch(`https://api.github.com/users/${username}`)
    .then((response) => response.json())
    .then(({ data }) => ({
      name: data.name,
      location: data.location,
      company: data.company,
      blog: data.blog.includes('https') ? data.blog : null
    }))
    .catch((e) => console.warn(e))
}


